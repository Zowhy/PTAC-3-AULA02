/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'ead.ifms.edu.br', "www.ifms.edu.br", "www.logo.wine"
        ]
    }
}

module.exports = nextConfig
